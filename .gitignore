node_modules
dist
dist-ssr
.env
.env.local
*.local
.DS_Store
.vite
*.tsbuildinfo
supabase/.temp/

supabase/.temp/
shot-mobile.mjs
shot-*.png

# 临时文件（含凭证/探针脚本，绝不入库）
_*.py
_*.json
_*.txt
posts.json
live2.js
_*.mjs

# 本地构建/巡检产物（绝不入库，避免仓库膨胀与凭证外泄）
dist_ux*/
dist_verify/
audit-shots/
ux-shots/
ux-shots2/
audit-result.json
*.log
vite.config.ts.timestamp-*.mjs
.chk.sql

# 一次性排查/冒烟测试用的 SQL（可能含 id 等，不入库）
.chk*.sql
.tmp*.sql
.top-*.sql
.cleanup.sql

# 本地 anon key（公开 key，但仍不入库，避免误当凭证用）
.anon
