# 择校通全平台代码审计（2026-08-24）

## 审计方法
- `npx tsc --noEmit` 全量类型检查（覆盖**所有** .ts/.tsx，能抓出打包器漏掉的未引用坏文件）
- 人工审查崩溃模式：null 访问、`.find()!` 非空断言、漏 `await`、`类型 ↔ 数据库字段` 不一致
- `npm run build` 最终验证

## 已修复的崩溃级 Bug（共 11 项，已本地提交 26fcf55）

| # | 位置 | 问题 | 后果 |
|---|------|------|------|
| 1 | `admin/Dashboard.tsx` | 读取 `store.orders`，但 store 从没定义 `orders` | 看板页 `undefined.reduce` **直接白屏** |
| 2 | `mobile/PublishTask.tsx` | `const r = publish(...)` **漏了 `await`** | 发布永远拿到 Promise，`r.ok` 为 undefined → 永远报 "undefined" 且不跳转 |
| 3 | `mobile/Placeholder.tsx` | 语法错误 `nav(admin ? '/admin' : ''/')` | 「返回」按钮白屏（打包未爆是因文件未被 import）|
| 4 | `types.ts` + TaskDetail/MyTasks/Mine | 接单字段 DB 是 `accepted_id`，类型却写 `accepted_by`，UI 跟着错 | "我接的单"列表为空、详情"联系接单者"指向 undefined |
| 5 | `lib/db.ts` | `Role` 未 import、`SupabaseClient` 来源错、`ensureUserId` 返回 `string\|null`、`reviewPass` 未用 `me` | 编译错误 + 潜在运行异常 |
| 6 | `lib/db.ts:425` | `createTask` 参数 `category: string` 与 `Task.category` 窄联合类型不符 | 编译错误（已断言修复）|
| 7 | `lib/types.ts` Comment | 缺 `author_avatar` 字段 | 评论带头像插入时被 TS 拒 |
| 8 | `store/store.ts:250` | 仲裁 `defendant_id: task.accepted_id`（`string\|null` ← `string\|undefined`）| 编译错误 |
| 9 | `AITangdou.tsx:242` | `error: s.error ?? null`（目标类型 `boolean\|undefined`）| 编译错误 |
| 10 | `AITangdou.tsx:527` | ChatMsg `role` 字面量推断为 `string` | 编译错误 |
| 11 | `DocWorkshop.tsx` / `lib/nav.ts` | 未使用 import；`NavDef.icon` 类型与 lucide 不匹配 | 编译错误 |

> 修复后：`tsc` 关键错误 **0**；生产构建 **通过**。
> 残留 33 个错误均为「未使用变量 / 隐式 any」严格模式噪音，不影响运行。

## ⚠️ 未决项（需你拍板）

### A. Supabase 迁移从未部署（最高优先级功能风险）
- `supabase/migrations/0001_init.sql` 和 `0002_comments.sql` 只在本地产物里。
- GitHub Actions **只构建前端**，不会跑 `supabase db push`。
- 如果之前没手动 push 过迁移 → 线上的 `tasks / goods / posts / comments / profiles` 等表**根本不存在** → 页面靠本地兜底跑空数据，评论功能静默降级（catch 兜底为空列表）。

**需要我执行 `supabase db push` 把表结构推到线上吗？**（需要项目凭证，会改线上数据库，建议确认后再做）

### B. 其他已知待完善（非崩溃）
- `Alerts`/`System 备份`：部分仍用占位逻辑
- `Placeholder.tsx` 是个"规划中"占位页（已修语法，但内容仍是"后续补全"）
- 钱包/提现目前是演示流（无真实支付网关）

## 建议下一步
1. 确认是否要我把本次审计修复 **push 部署** 到 `lwl555.github.io/zexiaotong`（你之前都是让我部署，这次只说了"看看"，所以先没动）
2. 如要，我再评估并执行 `supabase db push` 补上数据库表
