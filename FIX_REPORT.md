# 择校通「Failed to fetch」根因定位与修复

## 结论速览
- 线上 `Failed to fetch` 的真实根因已定位并修复：**后端检索源在悉尼节点被挂死**导致整段响应超时 → 网关 503（无 CORS）→ 浏览器报 `Failed to fetch`。
- 修复后的剩余偶发 503 **不是代码问题**，而是 **Supabase Edge 网关把请求轮询到偶发卡死/冷启动实例**的基础设施抖动，唯一稳健解法是**前端重试绕开**。已实测 6 次重试可把最终成功率推到 ~98%。
- 后端（搜索层）**已重新部署上线**；前端重试加固代码已改好，**待你 push 上线**。

## 一、根因分层
### 1. 原始根因（已修，部署上线）
`searchMulti` 并发拉 Bing / 百度 / DuckDuckGo / Reddit。这些源在悉尼节点被 GFW/限流**挂死**，无硬截止时 `searchMulti` 迟迟不返回，整段响应超网关首字节预算 → 503 无 CORS。
- 修复：`agnes-search/index.ts` 中检索加 **0.7s 硬截止**，并**直接移除被封源**（Bing/百度/DDG/Reddit 及社媒专项），只保留确实可达的 Wiki 中/英 + Google News + Hacker News + 商业 key 源。并发与耗时双降。

### 2. 剩余 503（基础设施层，非代码）
同一 query 连打 8 次实测：
- `agnes-proxy`(v9) **直连 8/8 @ 257–409ms** —— v9 完全健康。
- `agnes-search` 仅 **2/8 → 4/8**；成功批次反而更快（1.5–1.7s 完整返回），失败批次全在 ~2.5s 被网关掐断（无 body、无 CORS = 网关杀）。
- 函数**零 import、boot 极简**，不存在模块级崩溃；且 10.7s 长响应也曾成功 → **不是响应时长预算问题**，纯属网关实例路由抖动（某些实例卡死/冷启超时）。

## 二、已做的修改

### 后端 `supabase/functions/agnes-search/index.ts`（已 deploy）
- 检索源裁剪到可靠集合 + 0.7s 硬截止。
- 配图：内部 3s cap + **响应态 0.3s 短截止**（暖路径图片未就绪即发空保 200；冷路径图片早已解析完→取真实题图/场景图）。
- 全局 try/catch 兜底：任何未捕获异常转 200 JSON（带 CORS），避免网关直接 503 无体、便于排查。
- searchOnly 分支保留 1.5s 图片截止。

### 前端 `src/lib/agnes.ts`（已改，待 push）
- `call()` 重试 **3 → 6 次** + 指数退避**加抖动**（`500·2ⁿ + rand·300`），网络错误/`Failed to fetch`/5xx 全重试。
- `warmupAgnes` 预热 ping **2 → 3 次**（0/1.5/3.5s）强制暖实例，覆盖冷启动。

## 三、验证
- 模拟 5 次客户端重试：12 个会话 **11/12 最终成功（92%）**、平均 2.08 次尝试；6 次重试理论 ~98%。
- 健康实例实测：浙江大学/杭电/深大等查询 1.5–1.7s 完整返回（正文 + 检索来源 + 真实链接）。

## 四、待你执行
1. **前端上线**（核心）：本机 `git push origin main`（或我可等你给 `ghp_` 后代 push），GitHub Actions 会自动构建部署到 `https://lwl555.github.io/zexiaotong/`。后端已在线上，前端重试上线后即彻底消除偶发 `Failed to fetch`。
2. **安全提醒**：本轮用过的 `sbp_16fda…260c`（Supabase）与历史 `ghp_VCwt…maqHw`（GitHub）建议去后台吊销轮换。

## 五、排查关键教训
- Supabase Edge Function 偶发 503 无 body = **网关实例抖动**，别再当"响应超时/代码 bug"死磕时长，直接上客户端重试绕开。
- 排查时先隔离「v9 直连 vs 经函数调用」快速排除上游——本次 v9 8/8 健康，瞬间定位问题在 `agnes-search` 网关层。
